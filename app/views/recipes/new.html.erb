<h1>Create Recipe</h1>
<div><%= form_for @recipe do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %></div>
  Ingredients:
  <%= f.fields_for :ingredients do |i| %>
    <div><%= i.label :name %>
    <%= i.text_field :name %>
    <%= i.label :quantity %>
    <%= i.text_field :quantity %></div>
  <% end %>
  <%= f.submit %>
<% end %>
